name: Build Windows Executable

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable
        run: |
          pyinstaller --onefile --name onenote2md --hidden-import=customtkinter --hidden-import=tkinter onenote2md/gui.py

      - name: Create ZIP
        shell: powershell
        run: |
          Compress-Archive -Path dist\onenote2md.exe -DestinationPath dist\onenote2md.exe.zip -Force

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: onenote2md-windows
          path: dist/onenote2md.exe.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: onenote2md-windows

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: onenote2md.exe.zip
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
